{
    "minItems": 1, 
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "userId": { "type": "integer" },
            "orderNo": { "type": "integer" },
            "rating": { "type": "integer" },
            "comment": { "type": "string" },
            "ban": { "type": "integer" }
        },
        "required": ["userId", "orderNo", "rating", "comment", "ban"],
        "additionalProperties": false
    }
}
